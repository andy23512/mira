# Google serves the published CSV with CRLF. Storing it verbatim keeps a refetch
# from showing up as a whole-file diff; the parsers already ignore \r. `diff` keeps
# git treating it as text anyway, so the weekly refresh PR stays reviewable.
data/raw/*.csv -text diff
